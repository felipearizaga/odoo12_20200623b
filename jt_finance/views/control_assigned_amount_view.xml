<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- search view for calendar assigned amounts model -->
        <record id="calendar_assigned_amounts_search_view" model="ir.ui.view">
            <field name="name">calendar.assigned.amounts.search.view</field>
            <field name="model">calendar.assigned.amounts</field>
            <field name="arch" type="xml">
                <search string="Calendar of assigned amounts">
                    <field name="date"/>
                    <group expand="1" string="Group By">
                        <filter string="Budget" name='budget' context="{'group_by':'budget_id'}"/>
                        <filter string="Made By" name='user' context="{'group_by':'user_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- form view for calendar assigned amounts model -->
        <record id="control_amounts_received_form_view" model="ir.ui.view">
            <field name="name">control.amount.received.view.tree</field>
            <field name="model">calendar.assigned.amounts</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <group>
                                <!-- Fields for control of amount received -->
                                <field name="folio" readonly="1"/>
                                <field name="month_int" invisible="1"/>
                                <field name="budget_id" required="1"/>
                                <field name="file" filename="filename" readonly="1" attrs="{'invisible': [('file', '=', False)]}"/>
                                <field name="filename" invisible="1"/>
                                <button name="import_lines" type="object" string="Import lines" class="oe_highlight" attrs="{'invisible': ['|', ('month_int', 'not in', (3,6,9,12)), ('file', '!=', False)]}"/>
                               <!--  <button name="import_lines" type="object" string="Re-Import lines" class="oe_highlight" attrs="{'invisible': ['|', ('month_int', 'not in', (3,6,9,12)), ('diff', '!=', False)]}"/> -->
                                <field name="user_id" readonly="1"/>
                                <field name="import_date" readonly="1"/>
                                <field name="obs_cont_amount"/>

                                <!-- Fields for calender of amount received -->
                                <field name="date" invisible="1"/>
                                <field name="amount_to_receive" invisible="1"/>
                                <field name="amount_received" invisible="1"/>
                                <field name="amount_pending" invisible="1"/>
                            </group>        
                        </group>
                        <notebook>
                            <page string="Control of amounts received lines">
                                <field name="line_ids">
                                    <tree>
                                        <field name="calendar_assigned_amount_id" invisible="1"/>

                                       <field name="date"/>
                                       <field name="shcp_id"/>
                                       <field name="month"/>
                                       <field name="amount_assigned"/>
                                       <field name="amount_deposited"/>
                                       <field name="amount_pending"/>
                                       <field name="bank_id"/>
                                       <field name="bank_account_id"/>
                                       <field name="observations"/>
                                    </tree>
                                    <form>
                                        <group>
                                            <field name="calendar_assigned_amount_id" invisible="1"/>

                                            <field name="date"/>
                                            <field name="shcp_id" readonly="1"/>
                                            <field name="month" readonly="1"/>
                                            <field name="amount_assigned" readonly="1"/>
                                            <field name="amount_deposited"/>
                                            <field name="amount_pending"/>
                                            <field name="bank_id"/>
                                            <field name="bank_account_id"/>
                                            <field name="observations" readonly="1"/>
                                        </group>
                                    </form>
                                </field>
                            </page>
                            <page string='Import Status'>
                                <group>
                                    <field name="total_rows" readonly="1"/>
                                    <field name="diff" readonly="1"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- tree view for calendar assigned amounts model -->
        <record id="control_amounts_received_tree_view" model="ir.ui.view">
            <field name="name">control.amount.received.view.tree</field>
            <field name="model">calendar.assigned.amounts</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="folio"/>
                    <field name="month_int" invisible="1"/>
                    <field name="budget_id"/>
                    <field name="import_date"/>
                    <field name="user_id" readonly="1"/>
                    <field name="obs_cont_amount"/>

                    <!-- Fields for calender of amount received -->
                    <field name="file" invisible="1"/>
                    <field name="filename" invisible="1"/>
                    <field name="date" invisible="1"/>
                    <field name="amount_to_receive" invisible="1"/>
                    <field name="amount_received" invisible="1"/>
                    <field name="amount_pending" invisible="1"/>
                </tree>
            </field>
        </record>

        <!-- action for calendar assigned amounts views -->
        <record id="action_control_amounts_received" model="ir.actions.act_window">
            <field name="name">Control Of Amount Received</field>
            <field name="res_model">calendar.assigned.amounts</field>
            <field name="view_mode">tree,form,search</field>
            <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'tree', 'view_id': ref('control_amounts_received_tree_view')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('control_amounts_received_form_view')})]"/>
            <field name="context">{}</field>
        </record>
    </data>
</odoo>